\documentclass[12pt, a4paper]{report}

% Margins: 2.5 cm on all sides
\usepackage[margin=2.5cm]{geometry}

% Line Spacing: 1.5
\usepackage{setspace}
\onehalfspacing

% Bottom center page numbers starting from Chapter 1
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\cfoot{\thepage}
\renewcommand{\headrulewidth}{0pt} % No header line

% XeLaTeX font engine (required by polyglossia below).
\usepackage{fontspec}

% IMPORTANT load-order note: polyglossia (loaded last, for Arabic) pulls in the
% bidi package, and bidi requires graphicx/float/longtable/caption/hyperref to be
% loaded BEFORE it. So every bidi-sensitive package is loaded here, ahead of
% polyglossia, and polyglossia is the final package in the preamble.

% Figures and tables (bidi-sensitive: must precede polyglossia/bidi)
\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}

% Clickable links (bidi-sensitive: must precede polyglossia/bidi)
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
    pdftitle={Seed Bank Graduation Project Documentation},
    pdfpagemode=FullScreen,
}

% Captions (after hyperref/float, still before polyglossia/bidi)
\usepackage[font=small,labelfont=bf]{caption}
\captionsetup{skip=2pt,aboveskip=4pt,belowskip=2pt}
\graphicspath{{figures/}}
% Tighten vertical space around in-text [H] floats to keep the figure-heavy
% chapters compact (keeps the body within the page budget).
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\raggedbottom

% Typography: give the line breaker last-resort stretch so long unbreakable
% monospace runs (e.g. the routers->services->repositories->ORM chains and long
% file paths) don't overrun the right margin.
\setlength{\emergencystretch}{3em}
% Let long snake_case identifiers break at underscores so they wrap inside narrow
% table cells instead of overrunning. Adds only optional breakpoints.
\AtBeginDocument{%
  \let\sbOrigUnderscore\textunderscore
  \renewcommand{\textunderscore}{\sbOrigUnderscore\allowbreak}%
}

% Language support (XeLaTeX). polyglossia loads bidi for Arabic and must come
% LAST, after every bidi-sensitive package above.
\usepackage{polyglossia}
\setmainlanguage{english}
\setotherlanguage{arabic}
\gappto\captionsenglish{\renewcommand{\contentsname}{Table of Contents}}

% Fonts. TeX Gyre Termes (standard in TeX Live); Arabic is vendored in fonts/ so
% the build does not depend on a specific TeX Live install path and works in any
% XeLaTeX (full image, slim image, or a native install).
\setmainfont{TeX Gyre Termes}
\newfontfamily\arabicfont[
  Path=fonts/,
  Extension=.ttf,
  Script=Arabic,
  BoldFont=Amiri-Bold,
  ItalicFont=Amiri-Italic,
  BoldItalicFont=Amiri-BoldItalic
]{Amiri-Regular}
% polyglossia wants an Arabic monospace family whenever \ttfamily is used (e.g. in
% figure-placeholder boxes). We never set Arabic in monospace, so point it at Amiri
% to satisfy the check.
\newfontfamily\arabicfonttt[Path=fonts/, Extension=.ttf, Script=Arabic]{Amiri-Regular}

% References: IEEE style (BibLaTeX + biber). biblatex must load AFTER polyglossia,
% so it is the last package in the preamble. Needs the texlive-biblatex-ieee package.
\usepackage[style=ieee, backend=biber]{biblatex}
\addbibresource{references.bib}

% \figslot[width]{file}{caption}{label}
% Renders figures/<file> if it exists, otherwise a labelled placeholder box, so
% the document compiles whether or not the final image has been dropped in yet.
% Drop the real image into docs/report/figures/ with the exact <file> name and
% rebuild -- the placeholder is replaced automatically. See figures/README.md.
\newcommand{\figslot}[4][0.68]{%
  \begin{figure}[H]
    \centering
    \IfFileExists{figures/#2}{%
      \includegraphics[width=#1\textwidth,height=0.62\textheight,keepaspectratio]{figures/#2}%
    }{%
      \fbox{\parbox[c][0.13\textheight][c]{#1\textwidth}{\centering\ttfamily
        [\,FIGURE PLACEHOLDER\,]\\[6pt] figures/#2 \\[10pt]
        \normalfont\itshape #3}}%
    }
    \caption{#3}
    \label{#4}
  \end{figure}%
}

\newcommand{\figslotlarge}[4][0.9]{%
  \begin{figure}[H]
    \centering
    \IfFileExists{figures/#2}{%
      \includegraphics[width=#1\textwidth,height=0.88\textheight,keepaspectratio]{figures/#2}%
    }{%
      \fbox{\parbox[c][0.13\textheight][c]{#1\textwidth}{\centering\ttfamily
        [\,FIGURE PLACEHOLDER\,]\\[6pt] figures/#2 \\[10pt]
        \normalfont\itshape #3}}%
    }
    \caption{#3}
    \label{#4}
  \end{figure}%
}

\begin{document}

% ==============================================================================
% COVER PAGE
% ==============================================================================
\begin{titlepage}
    \begin{center}
        \vspace*{1cm}
        
        \large\textbf{Cairo University}\\
        Faculty of Computers and Artificial Intelligence\\
        Artificial Intelligence Department\\
        Information Systems Department
        
        \vspace{2cm}
        
        \huge\textbf{Seed Bank}\\
        \Large\textbf{Quality Intelligence Platform}
        
        \vspace{1.5cm}
        
        \Large\textbf{Graduation Project Final Documentation}
        
        \vspace{2cm}
        
        \textbf{Prepared by:}
        \begin{table}[h]
            \centering
            \begin{tabular}{cc}
                Omar Ezz-ElDein Abdullah & 20220228 \\
                Youssef Ahmed Awad & 20220385 \\
                Ali Abdulrahman Mohamed & 20220213 \\
                Youssef Tarek Ali & 20220397 \\
                Mohamed Amr Mahmoud & 20220302 \\
            \end{tabular}
        \end{table}
        
        \vspace{1.5cm}
        
        \textbf{Supervised by:} \\
        \begin{table}[h]
            \centering 
            \begin{tabular}{cc}
                Dr. Eman Ahmed \\
                Dr. Ali Zidane \\
                Dr. Heba Sherif \\
                Dr. Ghada Dahy \\
            \end{tabular}
        \end{table}
        \vfill
        
        Academic Year: 2025/2026
        
    \end{center}
\end{titlepage}

% ==============================================================================
% FRONT MATTER
% ==============================================================================
\pagenumbering{roman} % Roman numerals for front matter

\tableofcontents
\newpage

\listoffigures
\newpage

\listoftables
\newpage

% List of Abbreviations
\chapter*{List of Abbreviations}
\addcontentsline{toc}{chapter}{List of Abbreviations}
\begin{singlespace}
\begin{table}[h]
    \centering
    \begin{tabular}{ll}
        \textbf{API} & Application Programming Interface \\
        \textbf{CBAM} & Convolutional Block Attention Module \\
        \textbf{CNN} & Convolutional Neural Network \\
        \textbf{COCO} & Common Objects in Context (dataset format) \\
        \textbf{CPU} & Central Processing Unit \\
        \textbf{CSV} & Comma-Separated Values \\
        \textbf{FCAI} & Faculty of Computers and Artificial Intelligence \\
        \textbf{GMP} & Global Max Pooling \\
        \textbf{GPU} & Graphics Processing Unit \\
        \textbf{JSON} & JavaScript Object Notation \\
        \textbf{mAP} & mean Average Precision \\
        \textbf{ML} & Machine Learning \\
        \textbf{R-CNN} & Region-based Convolutional Neural Network \\
        \textbf{REST} & Representational State Transfer \\
        \textbf{RTL} & Right-To-Left \\
        \textbf{UI} & User Interface \\
        \textbf{YOLO} & You Only Look Once \\
    \end{tabular}
\end{table}
\end{singlespace}
\newpage

% Abstract
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}
\begin{spacing}{1.15}
\section*{English Abstract}
Seed quality decides how well a crop germinates and how much it finally yields,
yet it is still judged by hand. An inspector pours out a sample, spreads it on a
tray, and sorts the seeds into good and bad piles one at a time. That work is slow,
it tires the eye, and two people often disagree on the same seed, so it can only
ever check a tiny part of a large batch. Seed Bank is a prototype that does this
job automatically. A user takes a photo of a batch of seeds; the system finds
every seed in the picture, decides whether each one is good or bad, and reports how
many seeds there are, what share of them are good, and a breakdown by crop. It
currently handles two crops, coffee and maize. Teaching a computer to find the
seeds needs many example photos in which every seed is already marked, and those
are expensive to prepare by hand. A companion tool, MultiSeedGen, solves this by
building such training pictures automatically, so the system can learn without
anyone marking thousands of photos. On unseen test images the prototype finds
nearly every seed and grades each one correctly about 97\% of the time, and it does
so on an ordinary computer. This shows that fast, consistent, automatic seed
grading is within reach of the small laboratories and farmers who cannot afford
industrial sorting machines.

\vspace{1.5cm}

\section*{Arabic Abstract}
% otherlanguage* switches direction/font locally WITHOUT writing \selectlanguage
% into the .toc/.lof/.lot. The plain Arabic environment leaks those switches and
% flips the table of contents to RTL.
\begin{otherlanguage*}{arabic}
تُحدّد جودة البذور إلى حدٍّ كبير نسبة الإنبات والمحصول النهائي، ومع ذلك ما زال
تقييمها يجري يدويّاً. يسكب المُصنِّف عيّنةً من البذور، وينشرها على صينية، ويفرزها
بذرةً بذرة إلى سليمة وتالفة. هذا العمل بطيء ويُجهد العين، وكثيراً ما يختلف شخصان
على البذرة نفسها، ولذلك لا يمكنه فحص سوى جزء ضئيل من الدُّفعة الكبيرة. «Seed Bank»
نموذج أوّليّ يؤدّي هذه المهمّة تلقائيّاً؛ إذ يلتقط المستخدم صورةً لدُفعة من البذور،
فيعثر النظام على كلّ بذرة في الصورة، ويقرّر ما إذا كانت سليمة أو تالفة، ثمّ يعرض
عدد البذور ونسبة السليم منها وتوزيعاً بحسب نوع المحصول. ويتعامل النظام حاليّاً مع
محصولين، هما البُنّ والذُّرة. ويحتاج تعليمُ الحاسوب العثورَ على البذور إلى عدد كبير
من الصور التي وُسِمت فيها كلّ بذرة مسبقاً، وهي صور مكلفة الإعداد يدويّاً. تسدّ
الأداة المرافقة «MultiSeedGen» هذه الحاجة ببناء صور التدريب هذه تلقائيّاً، فيتعلّم
النظام دون أن يَسِم أحدٌ آلاف الصور. وعلى صور اختبار لم يَرَها النظام من قبل، يعثر
النموذج الأوّليّ على كلّ البذور تقريباً، ويصنّف كلّ بذرة تصنيفاً صحيحاً نحو 97٪ من
الحالات، ويفعل ذلك على حاسوب عاديّ. وهذا يبيّن أنّ تقييم جودة البذور تقييماً سريعاً
ومتّسقاً وتلقائيّاً صار في متناول المختبرات الصغيرة والمزارعين الذين لا يقدرون على
شراء آلات الفرز الصناعية.
\end{otherlanguage*}
\end{spacing}
\newpage

% ==============================================================================
% MAIN CHAPTERS
% ==============================================================================
\pagenumbering{arabic} % Bottom center page numbers starting from Chapter 1

\include{chapters/introduction}
\include{chapters/related_work}
\include{chapters/system_analysis}
\include{chapters/system_design}
\include{chapters/implementation}
\include{chapters/testing_evaluation}
\include{chapters/conclusion_future_work}

% ==============================================================================
% REFERENCES
% ==============================================================================
\printbibliography[heading=bibintoc, title={References}]

\end{document}
