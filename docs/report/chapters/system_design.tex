\chapter{System Design}

This chapter describes how the Seed Bank prototype is put together: the overall
architecture, the object model behind the inference pipeline, the main request
flow, the relational schema, the screens the user sees, and the security posture.
It also covers MultiSeedGen, the companion tool that generates the synthetic
training data the detector learns from. The design keeps the parts that change
for different reasons apart, so a new model, a new table, or a new screen can be
added without disturbing the rest \cite{cleanarchitecture2017}.

\section{System Architecture}

At a high level the prototype has five pieces. A React web client is the front end the user works with. A FastAPI backend exposes the API and runs the two-stage inference pipeline that turns a seed photo into a report \cite{fastapi}. A relational database (PostgreSQL) holds users, scan batches, and the per-seed results. An analytical database (ClickHouse) stores the inference metrics, scan statistics, and system telemetry for performance tracking. File storage keeps the uploaded images themselves, so the databases store references rather than image bytes.

Model inference is the heavy part of the work, so it does not run inside the
request that the web client is waiting on. The API accepts a batch, records it,
and hands the actual detection and classification off to a separate worker
service. The worker pulls the image, runs the pipeline, and writes the results
back to the database. This keeps the API responsive: a user submitting a batch
gets an immediate acknowledgement instead of waiting on a slow forward pass,
and several images can be graded in parallel by the worker.

\autoref{fig:context} places the system in its setting, showing the actors and the boundary they talk to.
\figslot[1.1]{01-system-context.pdf}{System context: the actors and the Seed Bank boundary they interact with.}{fig:context}

The worker side handles the actual inference. \autoref{fig:worker-flow} shows the task orchestration and worker process loop. The detailed pipeline itself is split in two: \autoref{fig:worker-pipeline-detect} shows detection through the persisted detect rows; \autoref{fig:worker-pipeline-classify} shows classification through batch finalization.
\figslotlarge[1.1]{04-worker-components-flow.pdf}{Inference worker task orchestration and dispatch.}{fig:worker-flow}

\figslot[1.1]{04-worker-components-pipeline-detect.pdf}{Inference worker pipeline: detection, from CAS pending → running through committing the detect rows.}{fig:worker-pipeline-detect}

\figslot[1.1]{04-worker-components-pipeline-classify.pdf}{Inference worker pipeline: classification, from cropping each detection through finalizing the batch.}{fig:worker-pipeline-classify}

\section{Class Diagrams}

The object model behind the inference pipeline is split into two views so
each stays readable at print size. \autoref{fig:class-ml} shows the ML
platform machinery: the \texttt{InferenceBackend} protocol implemented by
the three interchangeable engines (local PyTorch, Ultralytics YOLO,
Roboflow), the \texttt{ModelManager} that loads and LRU-caches weights, the
\texttt{ModelBuilder} registry that constructs an architecture before
weights are loaded into it, and the \texttt{ModelResolver} that decides
which model version actually runs a request.

\autoref{fig:class-domain} shows the request-facing side: \texttt{AnalysisService}
is the entry point that dispatches a scan and persists its state through
\texttt{ScanBatchRepository}, whose compare-and-set \texttt{cas\_status} keeps
concurrent workers from corrupting a batch's status. A backend (shown in
\autoref{fig:class-ml}) returns \texttt{Detection} and \texttt{Classification}
objects for each request; a \texttt{Detection} carries a \texttt{BoundingBox}.

\figslot[1.1]{13-class-ml-platform.pdf}{Class diagram: the \texttt{InferenceBackend} protocol and its three engines, backed by \texttt{ModelManager} for weight loading/caching and \texttt{ModelBuilder} for architecture construction, with \texttt{ModelResolver} selecting the production model.}{fig:class-ml}

\figslot[1.1]{13-class-domain-objects.pdf}{Class diagram: \texttt{AnalysisService} dispatching a scan through \texttt{ScanBatchRepository}, and the \texttt{Detection}/\texttt{Classification}/\texttt{BoundingBox} result objects a backend returns.}{fig:class-domain}

\section{Sequence Diagrams}

The main interaction is batch analysis, shown in \autoref{fig:analyze-seq}. When
a user submits a batch, the backend first identifies the user, then creates a new
\texttt{ScanBatch} record in the \texttt{PROCESSING} state. It iterates through
the uploaded files, saving each one to file storage and creating a matching
\texttt{ScanImage} entry.

Each image then goes through the pipeline. The detector locates every seed in the
image, and the backend records one \texttt{SeedDetection} per seed with its
bounding box. The system then crops each detected seed from the source image and
classifies it as good or bad. When every image is done it aggregates the
statistics across the batch, updates the status to \texttt{COMPLETED}, and returns
the structured report to the client.

\figslot[1.1]{06-analyze-sequence.pdf}{Batch-analysis sequence: identify the user, create the batch, save images, detect seeds, crop and classify each one, aggregate, and complete.}{fig:analyze-seq}

\section{Database Entity-Relationship Diagram}

The database schema is split into six focused views to preserve printed
legibility. \autoref{fig:erd-auth-identity} shows how a user authenticates
(password and Google OAuth) and how a refresh token is issued;
\autoref{fig:erd-auth-audit} shows the append-only audit log and the
suppliers a user creates. \autoref{fig:erd-scan-intake} shows how a
supplier's batch and its uploaded images are created; \autoref{fig:erd-scan-results}
shows what one image's inference produces, down to the per-seed detection.
\autoref{fig:erd-ml-registry} shows how a model version is registered and
scored; \autoref{fig:erd-ml-experiments} shows how a model is evaluated
offline against a labelled dataset.

\begin{minipage}{\linewidth}
The core scan hierarchy spans four central entities:
\begin{itemize}
    \item \texttt{USER} manages identity and access, and owns the scans a person creates.
    \item \texttt{SCAN\_BATCH} is the primary operational container. It tracks the progress of an analysis request (pending, processing, completed, or failed) and aggregates summary metrics (total seed count, bad-seed count, average confidence).
    \item \texttt{SCAN\_IMAGE} records the metadata for every individual file in a batch, including its storage path and its pixel dimensions.
    \item \texttt{SEED\_DETECTION} is the granular output of the models, storing a quality label (good or bad) and the normalized bounding box coordinates.
\end{itemize}
\end{minipage}

\clearpage
\figslotlarge[1.1]{05-db-erd-auth-identity.pdf}{Database schema: user identity --- password/OAuth login and refresh-token issuance.}{fig:erd-auth-identity}
\clearpage

\clearpage
\figslotlarge[1.1]{05-db-erd-auth-audit.pdf}{Database schema: audit trail and the suppliers a user creates.}{fig:erd-auth-audit}
\clearpage

\clearpage
\figslotlarge[1.1]{05-db-erd-scan-intake.pdf}{Database schema: scan batch intake --- suppliers, batches, and images.}{fig:erd-scan-intake}
\clearpage

\clearpage
\figslotlarge[1.1]{05-db-erd-scan-results.pdf}{Database schema: inference results --- images, model inferences, and seed-level detections.}{fig:erd-scan-results}
\clearpage

\clearpage
\figslotlarge[1.1]{05-db-erd-ml-registry.pdf}{Database schema: model registry --- versioned artifacts scoped by seed type and scored via performance metrics.}{fig:erd-ml-registry}
\clearpage

\clearpage
\figslotlarge[1.1]{05-db-erd-ml-experiments.pdf}{Database schema: offline evaluation --- labelled datasets, experiment runs, and per-item results against registered models.}{fig:erd-ml-experiments}
\clearpage

\section{MultiSeedGen Design}

Training a seed detector needs many photographs of cluttered, overlapping seeds
with a correct bounding box on every seed. These are slow to capture and tedious
to annotate by hand. MultiSeedGen is the companion tool that manufactures this
data. It takes photographs of single seeds and composites them into annotated
multi-seed scenes whose labels are correct by construction, an instance of
cut-and-paste synthesis \cite{dwibedi2017cutpaste}.

The pipeline begins by segmenting each seed out of its source photo into a clean
cut-out. A classical colour-and-brightness method is tried first, and a learned
matting model is the fallback, either U2-Net \cite{qin2020u2net} or Segment
Anything \cite{kirillov2023sam}. The clean cut-outs form a seed pool.

Scene composition then paints seeds onto a background. It samples a number of
seeds per scene and places each one with controlled placement, rejecting any
position that overlaps an existing seed too much or that leaves a seed too hidden
to label. Each placed seed gets its own augmentation (scale, rotation, and colour
jitter) chosen so it does not invalidate the label. The seeds are composited onto
varied backgrounds, from solid and textured fills to real inpainted photos. Some
camera-style degradation (blur, noise, and compression) is added so the synthetic
images resemble real phone photos, a form of domain randomization that helps a
model trained on them transfer to real input
\cite{tobin2017domainrand,shorten2019augmentation}.

Because the tool knows exactly where it placed each seed, it records the exact
bounding boxes and masks for every scene, accounting for seeds partly hidden
behind others. Export then writes the dataset in the formats a trainer expects:
YOLO boxes and segmentation polygons, and COCO annotations \cite{lin2014coco}. A
leakage-safe split makes sure a source seed reserved for validation never appears
in any training image, so the validation score is not inflated by the same seed
showing up on both sides. The resulting detection and segmentation datasets are
what train the Seed Bank detector.

\section{User Interface Design (Wireframes and Mockups)}

The prototype front end is a React web app that walks the user through the
analysis workflow, from the dashboard to the per-seed results and the saved
history. The screens below are the main views.

\subsection{Dashboard}

The dashboard is the central entry point for the workflow, shown in
\autoref{fig:web-dashboard}. It presents a prominent drag-and-drop zone for
uploading images, a few headline figures, and a list of recent scans, with a
status indicator that confirms the backend is reachable.

\figslot{screenshots/Dashboard.png}{Dashboard: the drag-and-drop upload zone, headline figures, and recent scans.}{fig:web-dashboard}

\subsection{Analysis and results workspace}

The analysis results workspace is where the user verifies the predictions, shown
in \autoref{fig:web-batch-detail}. The processed image is displayed with
color-coded bounding boxes, green for good seeds and red for bad seeds, so the
overall quality reads at a glance. A panel alongside the image gives aggregate
metrics, including the total detection count and the purity percentage. Below it,
a scrollable per-seed list details each detection with its confidence score. A
tabbed bar lets the user move between the images in a multi-image batch without
losing context.

\figslot{screenshots/web-batch-detail.png}{Analysis and results workspace: color-coded bounding boxes (green good, red bad), the per-seed list, and the purity panel.}{fig:web-batch-detail}

\subsection{Reporting and export}

The reporting view generates an exportable session summary, shown in
\autoref{fig:web-analyze}. It opens with high-level metrics, including a good
versus bad seed count shown as a pie chart and a per-image breakdown table that
points out skewing samples. Each image is detailed with its annotated bounding
boxes, and a final table lists every detected seed with a thumbnail, its quality
label, and its confidence score, so a reviewer can check the results seed by
seed.

\figslot{screenshots/seg-tuner.png}{Reporting and export view: high-level metrics, the good-versus-bad pie chart, the per-image breakdown, and the per-seed table.}{fig:web-analyze}

\subsection{Scan history}

The scan-history view is the repository for previously run analyses, shown in
\autoref{fig:web-login}. A statistical panel at the top aggregates global
figures, such as the total number of batches processed and the cumulative seed
count. Below it, each past session is a chronological card showing the timestamp,
the final status, and a good-versus-bad breakdown with the average confidence. A
filter sorts records by status, and a "View Details" action reopens the full
visual data and report for any archived session.

\figslot{screenshots/MobileView.png}{Scan history: the global statistics panel and the chronological session cards with filtering and per-session detail.}{fig:web-login}

\section{Security Design Considerations}

Security is brief and standard for a prototype of this size. Sessions use tokens:
the user signs in and the backend issues a short-lived access token that the
client sends with each request. Passwords are never stored in
plaintext; only their hashes are kept, so a database leak does not expose a usable
credential. Access is role-based, so an ordinary user can analyze and see only
their own history, while administrative and model-management actions are gated to
the roles that are allowed to perform them.
